<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>MaxConcurrentJobs__c</fullName>
    <defaultValue>50</defaultValue>
    <description>How many jobs we attempt to queue at once.   If we queue too many, jobs go to the flex queue, which reduces the effectiveness of the prioritization.</description>
    <externalId>false</externalId>
    <inlineHelpText>How many jobs we attempt to queue at once.   If we queue too many, jobs go to the flex queue, which reduces the effectiveness of the prioritization.</inlineHelpText>
    <label>Max Concurrent Jobs</label>
    <precision>3</precision>
    <required>true</required>
    <scale>0</scale>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
